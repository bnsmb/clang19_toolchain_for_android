# Set the CMake system version to "1" to disable CMake's built-in support for
# building with the Android NDK, which can interfere since we're not using the NDK.
set(CMAKE_SYSTEM_VERSION "1" CACHE STRING "")
